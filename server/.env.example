DATABASE_URL="postgresql://postgres:password@localhost:5432/helpdesk"
PORT=3001
BETTER_AUTH_SECRET="your-secret-here-min-32-chars"
BETTER_AUTH_URL="http://localhost:3001"
FRONTEND_URL="http://localhost:5173"
SEED_ADMIN_EMAIL="admin@example.com"
SEED_ADMIN_PASSWORD="your-seed-password"
GROQ_API_KEY="your-groq-api-key-here"
# Sentry (optional — leave blank to disable)
SENTRY_DSN=""
# SendGrid email (optional — leave blank to disable outbound email)
# Requires Single Sender Verification only — no custom domain needed.
# 1. Sign up at sendgrid.com and verify your Gmail address as a Single Sender.
# 2. Create an API key with "Mail Send" permission.
# 3. Set EMAIL_FROM to the exact verified sender email.
SENDGRID_API_KEY=""
EMAIL_FROM=""
# Inbound email webhook (optional — protects POST /api/webhooks/inbound-email)
# Set this in Cloudmailin dashboard under "Custom Headers" → x-webhook-secret
WEBHOOK_SECRET=""
